name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:14-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: smartbank_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
        
    - name: Backend - Install Dependencies
      run: |
        cd backend
        npm ci

    - name: Backend - Run Tests
      env:
        DATABASE_URL: postgres://postgres:password@localhost:5432/smartbank_test
        JWT_SECRET: testsecret
      run: |
        cd backend
        # We need to init DB for tests. 
        # For now, just run basic tests or lint if no tests
        npm test -- --passWithNoTests

    - name: Frontend - Install Dependencies
      run: |
        cd frontend
        npm ci

    - name: Frontend - Build
      run: |
        cd frontend
        npm run build
